#include<stdio.h>
#include<stdlib.h>

int main()
{
	int yaricap, yukseklik, alan, pi=3;
	printf("Yarýçapý giriniz:");
	scanf("%d",&yaricap);
	printf("Yuksekligi giriniz:");
	scanf("%d",&yukseklik);
	area =   pi* yaricap * yaricap * 2 + 2*pi*yaricap*yukseklik ;
	printf("Silindirin alaný: %d\n", alan);
	system("pause");
	return 0 ;
}
